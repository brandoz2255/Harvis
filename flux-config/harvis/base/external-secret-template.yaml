# This is a template for creating external secrets
# DO NOT commit this file with real values!
# 
# To use:
# 1. Copy this file to external-secret.yaml
# 2. Replace placeholders with real values  
# 3. Apply: kubectl apply -f external-secret.yaml
# 4. Add external-secret.yaml to .gitignore

apiVersion: v1
kind: Secret
metadata:
  name: harvis-api-keys
  namespace: ai-agents
type: Opaque
stringData:
  OLLAMA_API_KEY: "sk-a699ed1d359442abb13f9d4f84948cda"
  OPENAI_API_KEY: "YOUR_OPENAI_API_KEY_HERE"
---
# Example of how to reference the secret in HelmRelease
# Add this to your HelmRelease values:
#
# backend:
#   env:
#     OLLAMA_API_KEY: 
#       valueFrom:
#         secretKeyRef:
#           name: harvis-api-keys
#           key: OLLAMA_API_KEY
